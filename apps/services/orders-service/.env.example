# Orders service local development defaults
DB_HOST=localhost
DB_PORT=5434
DB_USER=orderly
DB_PASSWORD=orderly
DB_NAME=orderly_orders
TYPEORM_SYNC=true

# Kafka broker(s), comma separated
KAFKA_BROKERS=localhost:9092
KAFKA_CLIENT_ID=orders-service-local
ORDERS_CREATED_TOPIC=orders.order.created
